/**
 * Jonathan Limpus
 * Lab 4
 * Implement class SortedLinkedList which extends a LinkedList class
 * whose elements are placed into the list in ascending order. Override 
 * any methods from the LinkedList class as necessary. 
 */

package lab4;

public class SortedLinkedList<E> extends SinglyLinkedList<E> {
    private static class Node<E> {                              // Should be used for every node
        public void sortedAddFirst(E e) { 
        
        }
    }
}
